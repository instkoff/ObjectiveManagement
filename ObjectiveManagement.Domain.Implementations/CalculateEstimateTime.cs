namespace ObjectiveManagement.Domain.Implementations
{
    public class CalculateEstimateTime
    {
        //public int Calculate(ObjectiveModel objectiveModel)
        //{
        //    var subObjectives = objectiveModel.SubObjectives;
        //    var totalEstimate = 0;
        //    while (subObjectives != null)
        //    {
        //        totalEstimate += subObjectives.Select(o => o.EstimateTime).Sum();
        //        subObjectives.Where(x=>x.ParentId == objectiveModel.id)
        //    }
        //}
    }
}
